[project]
name = "knowledge-mcp"
version = "0.1.0"
description = "Read-only MCP server for local Pumpfun project knowledge."
requires-python = ">=3.11"
dependencies = [
    "mcp",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
